import { CheckboxField } from "./CheckboxField";

export enum FieldType {
    InputField,
    TextAreaField,
    DateField,
    EmailField,
    SelectField,
    FieldLabel,
    CheckboxField,
    SelectOptionField
}

